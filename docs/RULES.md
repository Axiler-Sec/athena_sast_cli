# Local OWASP CI/CD rules (ATH001–ATH054)

Reference: https://cheatsheetseries.owasp.org/cheatsheets/CI_CD_Security_Cheat_Sheet.html

Run `athena rules` and `athena owasp` for the live catalog. Engine findings use a separate `source: engine` tag and may omit `owasp_id` when no CWE mapping exists.
